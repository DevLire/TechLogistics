import { ThemedView } from '@/presentation/components/ThemedView';

const BloqueoScreen = () => {
  // const isDeviceRegistered = false;
  // const isNewDeviceRegisterEnabled = false;
  return (
    <ThemedView className="flex-1">
      <ThemedView margin className="mt-20 items-center gap-2"></ThemedView>
    </ThemedView>
  );
};

export default BloqueoScreen;
